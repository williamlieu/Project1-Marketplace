<h1><%= @product.title %></h1>
<%= image_tag @product.image, :alt => @product.title, :class => 'feature'%>

<ul>
  <li><%= @product.title %></li>
</ul>


<div class="controls">
  <%= link_to 'Edit', edit_product_path(@product) %>
  <%= link_to 'Delete', product_path(@product), :method => 'delete' %>

</div>
