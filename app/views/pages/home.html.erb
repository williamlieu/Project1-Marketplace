<div class="jumbotron">

<div class="container">
  <h1>Sell me</h1>
  <p>The best free local classifieds site to buy and sell your: cars, furniture, clothes, books, games, computers, and electronics!</p>
  <p><a class="btn btn-primary btn-lg" href="#" role="button">Get Started</a></p>
</div>

</div>

 <% if @current_user.present? %>
   <h1>Welcome back  <%=  @current_user.email %></h1>

<% end %>

<div class="row">
    <% Product.all.each_with_index do |p, i| %>
    <div class="col-sm-4">
      <div class="productitem<%= i+1 %>">
        <%= image_tag p.image, :class => "productitem#{ i+1 }__img" if p.image.present? %>
        <h2 class="productitem<%= i+1 %>__title"></h2>
        <%= link_to p.title, product_path(p), :class => "productitem#{i+1}__price" %>
        <span class="productitem<%=i+1%>__price"><%=  p.price %></span>
      </div>
    </div>
    <% end %>
</div>


<footer class="container-fluid text-center">
  <p>Sell me Copyright</p>
  <form class="form-inline">Get newsletter:
    <input type="email" class="form-control" size="50" placeholder="Email Address">
    <button type="button" class="btn btn-danger">Sign Up</button>
  </form>
</footer>
